# backtest

## 目的
提供回测相关能力，用于在历史数据上验证策略/规则的行为与指标。

## 模块概述
- **目录:** `backtest/`
- **职责:** 回测运行、历史数据回放、结果统计/输出（以代码为准）
- **状态:** 📝规划中（文档）
- **最后更新:** 2026-01-11

## 规范
回测的输入、输出、指标口径与可复现要求，应在后续变更中以“可复现示例 + 测试”形式固化，并同步更新本文件。

## API接口
- 如对外提供回测任务/报告接口，契约应维护在 `helloagents/wiki/api.md`。

## 数据模型
- 如涉及回测结果存储、指标表结构，维护在 `helloagents/wiki/data.md`。

## 依赖
- 可能依赖 `market/`（历史行情）、`data/`（数据处理）、`store/`（持久化），以代码为准。

## 变更历史
- 暂无（从 `helloagents/history/` 开始记录）

