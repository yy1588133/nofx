# provider

## 目的
提供外部系统/服务/数据源的适配层，隔离第三方差异并统一内部调用方式。

## 模块概述
- **目录:** `provider/`
- **职责:** 第三方 API 适配、协议转换、重试/限流等（以代码为准）
- **状态:** 📝规划中（文档）
- **最后更新:** 2026-01-11

## 规范
第三方依赖变更应重点记录：
- 版本与兼容性（含许可证/约束）
- 失败重试、限流、熔断与超时策略
- 关键接口的最小可复现验证方式

## API接口
- 对外接口契约统一维护在 `helloagents/wiki/api.md`。

## 数据模型
- 如涉及第三方返回字段映射与内部模型，维护在 `helloagents/wiki/data.md`。

## 依赖
- 常与 `market/`、`trader/` 交互，以代码为准；变更时更新本节。

## 变更历史
- 暂无（从 `helloagents/history/` 开始记录）

