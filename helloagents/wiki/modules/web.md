# web

## 目的
提供前端界面与交互层，用于展示系统状态、配置与操作入口（以实际实现为准）。

## 模块概述
- **目录:** `web/`
- **职责:** UI 与交互逻辑、静态资源、前端工程化（以代码为准）
- **状态:** ✅可用（持续补齐文档）
- **最后更新:** 2026-01-17

## 规范
前端相关变更应关注：
- 与后端接口契约一致（同步更新 `helloagents/wiki/api.md`）
- 关键交互路径可回归（建议补齐自动化测试或最小可复现步骤）
- 安全：避免在前端代码中暴露敏感信息

## API接口
- 依赖的接口契约维护在 `helloagents/wiki/api.md`。

## 数据模型
- 前端使用的数据结构口径以 `helloagents/wiki/data.md` 为准（如适用）。

## 依赖
- 与 `api/` 的交互契约以代码为准。

## 变更历史
- 2026-01-17 - 修复前端类型定义与后端字段不一致导致的构建失败（`RiskControlConfig`、`Exchange`）。
